const config = {
  displayedHours: 12, // number of hours displayed in calendar
  hoursInPixels: 60, // size of one hour in pixel
  firstDisplayedHour: 9, // first timeslot visible
};

export default config;
